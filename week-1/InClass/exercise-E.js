const greetingStart = "Hello, my name is ";
const name = "Keti";

const greeting = greetingStart + name;

console.log(greeting);                  // the plus operator to concat strings
console.log(`${greetingStart}${name}`); // the String interpolation to concat strings